# 毫米波与 Sub-7 协同接入仿真 v2 使用说明

## 1. 已锁定的实验定义

- 正式扫描只允许 `M=1:1:6`，数据时长为 `Tp=190*M us`。
- 六个协议共用同一条 5 us 物理到达轨迹；到达发生在协议决策之前。
- 主口径为固定 `pkt/STA/s`；补充口径为 `lambda_effective=lambda_base/M` 的固定归一化业务量。
- SF-CF、SF-CB 使用经典碰撞：同一竞争边界两个及以上发送者全部失败，不使用天线、捕获或 SINR。
- SB-CF、SB-CB 使用方向性 CCA、AP 端 SINR、最强可解码单用户捕获以及 CTS/NAV 物理模型。
- 所有 CSMA 新 HOL、成功后的下一 HOL和失败重试均从零等待完整 15 us DIFS；到达前的空闲时间不能抵扣。
- S7-AS 使用理想全向 Sub-7 碰撞信道；接入映射到 9 us 边界，但所有 MLO/SLO 数据均保持精确 `Tp`。
- 完成区间统一采用 `[start,end)`，完成时刻是发送区间末端。

旧的 `proto_*.m` 文件保留用于复现历史结果及 `M=0.1` 诊断，不再作为正式实验入口。

## 2. 推荐入口

### 冒烟验证

```matlab
cfg = default_experiment_config('smoke');
experiment = run_experiment(cfg);
analysis = analyze_experiment_v2(experiment.output_dir);
```

### 相对完整分析（当前推荐）

```matlab
cfg = default_experiment_config('analysis');
experiment = run_experiment(cfg);
analysis = analyze_experiment_v2(experiment.output_dir);
```

也可以直接运行 `run_analysis_1s`。当前 `analysis` 配置采用 0.2 s
warm-up、1 s正式测量、最多1 s排空、2个调优种子和2个独立评估种子；
调优基础测量时间为0.5 s，低到达率条件最多自动延长到1 s。它仍保留
六协议、`lambda=[5 10 15]`、`M=1:6`和两种负载口径，不重复CCA和拓扑
消融。

`analysis`保留六协议、`lambda=[5 15 30]`、`M=1:6`和两种负载口径，
采用20 ms warm-up、100 ms测量、100 ms排空、2个调优种子和2个独立评估
种子。调优按聚合负载选择q网格：轻载扫描`[0.05 0.1 0.2 0.5 1]`，
中载扫描`[0.025 0.05 0.1 0.2]`，重载扫描`[0.01 0.025 0.05]`；低到达率
调优自动延长到至少约20个期望到达、最多200 ms。它不重复CCA与拓扑消融，适合在
`scaled`之后快速获得比单种子结果更稳健的主矩阵，但仍不替代`full`。

### 中等规模试验

```matlab
cfg = default_experiment_config('pilot');
experiment = run_experiment(cfg);
analysis = analyze_experiment_v2(experiment.output_dir);
```

### 全轴缩短验证（推荐在正式长跑前执行）

```matlab
cfg = default_experiment_config('scaled');
experiment = run_experiment(cfg);
analysis = analyze_experiment_v2(experiment.output_dir);
```

`scaled` 会覆盖六协议、`lambda=[5 15 30]`、`M=1:6` 和两种负载口径，
并执行小规模 CCA 消融及 SF-CB/SB-CB 拓扑复核。它用于发现逻辑、接口和
性能问题。当前配置为5 ms warm-up、50 ms测量、100 ms排空、每点1个评估
run，工程调优只扫描 `q=[0.01 0.025 0.05]`；由于窗口、重复数和q网格都较短，
不能把其稳定边界或置信区间当作正式论文结论。2026-07-23完成的推荐工程
结果见 `results_v2/RESULTS_INDEX.md`。

### 完整正式试验

```matlab
cfg = default_experiment_config('full');
experiment = run_experiment(cfg);
analysis = analyze_experiment_v2(experiment.output_dir);
```

`full` 配置包含：2 s warm-up、10 s测量、最多50 s排空、10个独立评估流量种子、CCA/灵敏度消融和5个独立拓扑鲁棒性点。q调优另用3个独立种子和2 s测量，不会用正式评估种子选q。

SB-CB 的逐5 us物理状态、逐节点反事实CCA和扇区CTS诊断仍是正式长跑的主要
耗时项。`condition_timeout_s` 当前是完成后的超时标记与告警，不会在条件中途
强制杀死仿真；因此不要依赖它限制墙钟时间。建议先跑 `scaled`，再用 `full`
按输出目录断点续跑。当前实现尚未用完整矩阵实证达到3--8小时目标。

## 3. 常用自定义配置

```matlab
cfg = default_experiment_config('pilot');
cfg.protocols = {'sf_cb','sb_cb'};
cfg.lambda_values = [5 15 30];
cfg.M_values = 1:6;
cfg.load_modes = {'fixed_packet','fixed_payload'};
cfg.payload_bits_M1 = NaN;       % 未知时不虚构bit/s
cfg.cca_mode = 'directional';    % directional | oracle | disabled
cfg.rx_sens_dbm = -62;
cfg.parallel = true;
cfg.n_workers = 4;
experiment = run_experiment(cfg);
```

若已知 `M=1` 包含的有效载荷位数，可设置 `payload_bits_M1`，此时自动输出 `goodput_bit_s`；否则该字段保持 `NaN`。始终输出：

```text
normalized_goodput_units_s = M * completed_packets_per_second
```

## 4. 兼容入口

以下原有函数名仍可调用，但已全部重定向到 v2，不再写入或覆盖旧 `results/`：

```matlab
script_aloha_slot(15,2e6,3)   % SF-CF
script_aloha_conn(15,2e6,3)   % SF-CB
script_csma_basic(15,2e6,3)   % SB-CF
script_csma_rts(15,2e6,3)     % SB-CB
script_sub7_clean(15,2e6,3)   % S7-AS, nS=0
script_sub7_busy(15,2e6,3)    % S7-AS, nS=10
run_all_delay([5 15 30],2e6,3)
```

## 5. 输出目录和续跑

每次运行写入：

```text
results_v2/<timestamp>_<config-and-code-hash>/
```

主要内容：

- `manifest.json`：配置哈希、源码指纹、MATLAB版本、验证状态和 Material Passport；
- `config.mat`、`scenario.mat`：完整配置、拓扑和功率矩阵；
- `checkpoints/`：每个主条件的q调优与独立评估结果；
- `summary.csv`、`experiment.mat`：主汇总；
- `cca_ablation.csv`、`topology_robustness.csv`：完整配置下的扩展实验；
- `theory_validation.csv`、`aloha_capacity_theory.csv`：预约概率与稳定容量理论；
- `csma_diagnostics.csv`、`cca_ablation_diagnostics.csv`：主实验和监听消融诊断；
- `topology_cluster_ci.csv`：以拓扑为独立聚类单位的置信区间；
- `figures/`、`中文理论-仿真报告.md`：图和中文理论—仿真对照报告；其中
  `delay_by_M.png` 只画稳态均值，`delay_by_M_with_unstable.png` 还会用
  三角形/叉号画出 mixed/unstable 条件下“截止前已完成包”的条件时延，
  该值不能解释为稳态平均时延；
- `verification/test_report.json`：自动确定性测试结果。
- `acceptance_checks.csv`：严格5%速率/Little门与受控Aloha硬门；在`scaled`
  短窗口下的速率/Little失败首先表示统计窗口不足，不等同于包守恒失败；
- `results_v2/RESULTS_INDEX.md`：区分推荐结果、专项诊断、中止扫描和开发期目录。

中断后续跑：

```matlab
cfg = default_experiment_config('full');
cfg.output_dir = '此前输出目录的完整路径';
cfg.resume = true;
experiment = run_experiment(cfg);
```

配置哈希排除输出路径、并行worker数等运行位置参数，但包含仿真源码指纹。目录、配置或代码不匹配时会拒绝混用旧检查点。

## 6. 时延、稳定性和守恒指标

每包记录：

```text
arrival, HOL_start, first_attempt, completion, attempts, status
```

并严格验证：

```text
queue_delay  = HOL_start - arrival
access_delay = completion - HOL_start
total_delay  = completion - arrival
total_delay  = queue_delay + access_delay
```

接入时延进一步分解为边界等待、DIFS、概率等待、忙/NAV等待、失败/碰撞、预约控制和数据阶段。未完成包标记为 censored，不参与有限稳态时延；但会报告删失数、最终积压、完成率和后半程 backlog slope。

只有全部独立评估run均稳定时，`mean_delay_us`、p50/p95/p99等稳态字段才有有限值。`conditional_mean_delay_us`只描述有限窗口内已完成包，不能在不稳定条件下当作稳态均值。

碰撞时间有两个不可混淆的口径：

- `collision_channel_time_us`：碰撞/失败区间并集的墙钟时间；
- `collision_tx_airtime_us`：所有失败发送者空口时间之和，多发送者碰撞时可更大。

## 7. Conn-CSMA监听诊断

原始监听错误率保留用于复现旧图，但它会计入空队列、未完成DIFS、NAV保护和无害方向性耳聋。

有效CCA只在有HOL、IDLE且NAV=0的机会统计。SB-CB逐节点反事实计算：若该节点此刻发RTS，是否会把当前仍可解码的RTS、当前扇区CTS或数据接收推到不可解码。输出TP/FN/FP/TN、自身链路不可解码、控制/数据/RTS有害机会、晚启动、捕获、ICR丢失、NAV遗漏和碰撞浪费。

## 8. 自动验证和专项诊断

```matlab
report = run_v2_tests();               % 确定性时序、守恒、CCA和删失测试
validation = validate_aloha_theory_v2(); % Ps=Kq(1-q)^(K-1)与服务周期
legacy = diagnose_legacy_M01();        % 旧M=0.1现象；不进入正式扫描
```

理论校验若需要消除190 us相位等待，可单独生成190 us边界到达；正式公平对比仍必须使用共同5 us物理到达，不能让每个协议按自己的slot另造一套流量。

## 9. 关键文件

- `default_experiment_config.m`：配置；
- `run_experiment.m`：调优、独立评估、并行、检查点与扩展实验；
- `generate_arrival_trace.m`：共同5 us稀疏到达；
- `simulate_aloha_v2.m`：SF-CF/SF-CB；
- `simulate_sb_cf_v2.m`：SB-CF；
- `simulate_sb_cb_v2.m`：SB-CB；
- `simulate_s7_v2.m`：两种S7-AS；
- `finalize_sim_result.m`：统一统计与验收；
- `analyze_experiment_v2.m`：绘图、理论概率、监听诊断和中文报告。

## 10. 只补跑非稳定条件

`analysis` 档位使用协议专属 q 网格。SF-CF、SF-CB 和 S7 的竞争决策以
190 us 边界为主；SB-CF、SB-CB 每 5 us 都可能产生竞争机会，因此不能
共用同一 q 数值范围。当前网格在 `default_experiment_config.m` 中显式保存，
且不包含会在多节点积压时形成吸收碰撞态的 `q=1`。

对于 2026-07-23 的 1 秒基础结果，可运行：

```matlab
rerun_unstable_1s
```

脚本只选择基础 `summary.csv` 中 `stable_fraction<1` 的条件。补跑完成后，
`merge_supplement_results` 会从 CSV 重新读取基础表和补跑表以统一文本列类型，
并执行以下合并：

1. 原 216 个 checkpoint 复制到 `combined_view/checkpoints/`；
2. 用补跑的同名 checkpoint 覆盖对应条件；
3. 生成带 `result_source` 和 `rerun_replaced` 标记的
   `combined_summary.csv`；
4. 对完整 216 条 combined 结果重新生成理论、监听、验收、图和中文报告。

若补跑仿真已经完成、仅合并或绘图后处理失败，不要重新计算协议仿真，可单独：

```matlab
base_dir = fullfile(pwd,'results_v2','20260723_160326_a80db2f32f0f');
rerun_dir = fullfile(pwd,'results_v2','20260723_200440_dae2255a7e44');
outputs = merge_supplement_results(base_dir,rerun_dir);
```

1 秒只是测量窗口长度，不会让真实过载条件“变稳定”。短窗口反而会增加种子
波动：当前 `analysis` 每点只有 2 个独立评估 run，只要一个 run 未通过，
`stable_fraction` 就是 0.5；调优阶段要求 3/3 种子稳定，否则不产生
`best_q`。因此 mixed、无 q 和严格 5% 验收失败必须分别报告。
